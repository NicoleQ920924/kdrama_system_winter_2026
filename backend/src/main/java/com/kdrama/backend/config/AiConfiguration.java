package com.kdrama.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AiConfiguration {
    // AI Configuration is handled by Spring AI auto-configuration
    // Gemini API key is configured via: spring.ai.google.ai.gemini.api-key in application.properties
}

